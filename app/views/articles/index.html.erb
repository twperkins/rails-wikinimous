<div class="list">
  <% @articles.each do |article| %>
    <div class="element">
      <h2><%= link_to article.title, article_path(article), class: 'article-link' %></h2>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
